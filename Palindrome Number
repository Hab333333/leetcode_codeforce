class Solution:
    def isPalindrome(self, x: int) -> bool:
       x =str(x) 
       z = x[::-1]
       flag = int(0)
       for i in range(len(x)):
           if x[i] == z[i]:
               flag = flag +1
       if flag == len(x):
           return True  
       else:
           return False
